<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
	<div class="container">
		<a href="/" class="navbar-brand">rkwt</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
			<span class="navbar-toggle-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mobile-nav">
			<ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path("item_list") }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'item' %} active{% endif %}"><i class="fas fa-th-list"></i> Przedmioty</a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle{% if app.request.attributes.get('_route') starts with 'search' %} active{% endif %}" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                          <i class="fas fa-search"></i> Wyszukiwania
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                          <a class="dropdown-item" href="{{ path("search_list") }}">Lista</a>
                          <div class="dropdown-divider"></div>
                          <a class="dropdown-item" href="{{ path("search_new") }}">Dodaj nowe</a>
                        </div>
                    </li>

                    <li class="nav-item">
                        <a href="{{ path("user_settings") }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'user' %} active{% endif %}"><i class="fas fa-cogs"></i> Ustawienia</a>
                    </li>

                    <li class="nav-item">
                        <a href="{{ logout_url() }}" class="nav-link"><i class="fas fa-sign-out-alt"></i> Wyloguj</a>
                    </li>








                {% else %}
                    <li class="nav-item">
                        <a href="{{ path("login") }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'login' %} active{% endif %}"><i class="fas fa-sign-in-alt"></i> Logowanie</a>
                    </li>
                {% endif %}
			</ul>
		</div>
	</div>
</nav>