<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
	<div class="container">
		<a href="/" class="navbar-brand">rkwt</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
			<span class="navbar-toggle-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mobile-nav">
			<ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path("item_list") }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'item' %} active{% endif %}">Przedmioty</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path("search_list") }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'search' %} active{% endif %}">Wyszukiwania</a>
                    </li>
    				<li class="nav-item">
    					<a href="{{ logout_url() }}" class="nav-link">Wyloguj</a>
    				</li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path("login") }}" class="nav-link">Logowanie</a>
                    </li>
                {% endif %}
			</ul>
		</div>
	</div>
</nav>